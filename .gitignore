# Cargo build output.
/target/
**/*.rs.bk
*.pdb

# Nix build output (nix build symlinks, direnv cache).
result
result-*
.direnv/

# Release artifacts (release.sh).
/ferritor-*.tar.xz

# Profiling output.
perf.data*
flamegraph.svg

status.yaml

# Local cloned/reference repos (session-only).
comrak/
egui-file-dialog/
shadcn-rs/
ink/
markdown-table-formatter/
ob-table-enhancer/
rustdown/
skim/

# Ignore markdown notes/docs.
*.md

# Ignore dot-directories anywhere in the repo.
**/.*/
