objective: "Ship Ferritor as a daily-use desktop filesystem browser and text editor with GTK-derived visuals, syntax-highlighted viewing/editing, and a working release path."
state: "Filesystem navigation, edit/save flow, terminal launch, TypeScript/JavaScript fallback highlighting, and release packaging are wired and passed human smoke on 2026-03-17."
next_steps:
  - "Replace the path-entry open-directory dialog with a true native folder picker if desired."
  - "Add a lightweight Ferritor state/index file for recents, reopen state, and future file-history affordances."
  - "Decide whether non-UTF-8 and very large files should get a dedicated handling path instead of the current text-only failure message."
manual_smoke:
  status: "passed"
  date: "2026-03-17"
  command: "nix develop -c cargo run"
  notes:
    - "John confirmed .md and .rs files open and edit correctly."
    - "John confirmed the path-entry open-directory flow works once focused."
    - "John confirmed the file-tree cursor-lock regression was fixed."
    - "John confirmed folder button, keyboard shortcuts, and terminal launch all work in the current directory."
known_gaps:
  - "Open-directory is still a focused path-entry dialog, not a native folder picker."
  - "The editor currently assumes UTF-8 text files and reports a load error for non-text content."
  - "Old DotAgent-era scanner/sync source files and traverse docs still exist on disk as legacy context."
human_blockers: []
